export const title = {
    fontSize: '40px',
    fontWeight: 'bold',
    fontFamily: "'Times New Roman', Times, serif",
    color: '#000',
    marginLeft: 38,
  };

export const box = {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    gap: '20px',
    padding: '20px',
    margin: '20px',
    borderRadius: '10px',
    boxShadow: '0 14px 28px rgba(0, 0, 0, 0.25), 0 10px 10px rgba(0, 0, 0, 0.22)',
    backgroundColor: '#fff',
    width: '200px',
    height: '200px',
    textAlign: 'center',
    marginLeft : 40,
}

export const convert = {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    gap: '20px',
    padding: '20px',
    margin: '20px',
    borderRadius: '10px',
    boxShadow: '0 14px 28px rgba(0, 0, 0, 0.25), 0 10px 10px rgba(0, 0, 0, 0.22)',
    backgroundColor: '#fff',
    width: '200px',
    height: '250px',
    textAlign: 'center',
}

export const titleConvert = {
    fontSize: '40px',
    fontWeight: 'bold',
    fontFamily: "'Times New Roman', Times, serif",
    color: '#000',
}

export const label1 = {
    fontSize: '20px',
    fontFamily: "'Times New Roman', Times, serif",
    color: '#000',
}

export const trackerTitle = {
    fontSize: '40px',
    fontWeight: 'bold',
    fontFamily: "'Times New Roman', Times, serif",
    color: '#000',
    padding : 2,
    marginLeft: 40,
  };


export const calendar = {
    display: 'flex',
    marginLeft: '10px',
    border: '1px solid black',
    borderRadius: '10px',
};

export const date = {
    fontSize: '30px',
    fontWeight: 'bold',
    fontFamily: "'Times New Roman', Times, serif",
    color: '#000',
};

export const userBox = { 
    flex: 1, 
    borderRadius:'5px', 
    width:'auto', 
    height : 'auto'
};

export const user = {
    fontSize: '20px',
    fontFamily: "'Times New Roman', Times, serif",
    color: '#000',
};
